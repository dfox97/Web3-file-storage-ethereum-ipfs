import { DocUploadFormComponent } from './doc-upload-form.component';

describe('HomePageComponent', () => {
  let component: DocUploadFormComponent;

  beforeEach(() => {
    component = new DocUploadFormComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
